# MongoDB连接配置
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_DB_NAME=app-market

# 注意：
# 1. 请将此文件复制为.env文件
# 2. 替换<username>为你的MongoDB用户名
# 3. 替换<password>为你的MongoDB密码
# 4. 替换<cluster>为你的MongoDB集群地址
# 5. 替换<database>为你的数据库名称